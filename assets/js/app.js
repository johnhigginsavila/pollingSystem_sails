/*var socket = io.connect();
console.log(socket);
io.socket.request({
	method:'get',
	url:'/session/create',
	
}, function(resData, jwres){
	console.log(jwres);
})*/

/*io.socket.get('/user/subscribe/', function responseFromServer(body, response){
	console.log("The server responded with status " + response.statusCode + " and said: ", body);
});

io.socket.on('user', function(event){
	switch (event.verb){
		case 'updated':
			console.log(event);
			break;
		default:
			console.log('Unrecognized socket');
	}
})*/